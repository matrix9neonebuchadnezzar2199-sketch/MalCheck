# Copy to compose/.env and adjust

ANALYSIS_TIMEOUT=600
MAU_LOG_LEVEL=INFO

# Orchestrator → surface container name (docker exec)
SURFACE_CONTAINER=surface-analyzer
REMNUX_CONTAINER=surface-analyzer

# Paths (deploy scripts generate .env.runtime)
PROJECT_ROOT=
SAMPLES_DIR=
RESULTS_DIR=
SCRIPTS_DIR=
RULES_DIR=
CONFIG_DIR=
